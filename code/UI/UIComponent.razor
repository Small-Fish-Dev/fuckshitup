@namespace FUCKSHIT.UI
@inherits PanelComponent

<root>
	<Tabview />
	@*<Popup Size=@(Vector2.One * 500) Position=@(Screen.Size / 2f - 500 / 2f) />*@
</root>

@code {
	public static UIComponent Instance { get; private set; }

	protected override void OnStart()
	{
		base.OnStart();

		DragItemPanel.Reset();
		Instance = this;
	}
}
